/* Services */
"use strict";

angular.module('EducationEntity', ['ng-admin'])
.factory('Education', ['NgAdminConfigurationProvider', function(nga) {
  return {
    get: function(lang){

      // add the user entity to the admin application
      return ;
    }
  };
}]);
